<!DOCTYPE html>
<html lang="en" class="h-full bg-gray-900">
<head>
    <title>Welcome Home</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="h-full">

    <div class="flex min-h-full flex-col items-center justify-center px-4 py-12 sm:px-6 lg:px-8">
      
      <div class="text-center">
        <h1 class="text-4xl font-extrabold text-white mb-6">Welcome Home</h1>

        <% if(user) { %>
            <p class="text-lg text-gray-300 mb-8">
                Logged in as <span class="font-semibold text-indigo-400"><%= user.username %></span>.
            </p>
            <div class="flex justify-center space-x-4">

                <a href="/dashboard" class="px-4 py-2 bg-indigo-600 text-white font-semibold rounded-md shadow-md hover:bg-indigo-700 transition duration-150">
                    Go to Dashboard
                </a>

                <a href="/logout" class="px-4 py-2 bg-gray-600 text-white font-semibold rounded-md shadow-md hover:bg-gray-700 transition duration-150">
                    Logout
                </a>
            </div>
            
        <% } else { %>
            <p class="text-lg text-gray-300 mb-8">
                Please log in or register to continue.
            </p>
            <div class="flex justify-center space-x-4">
    
                <a href="/login" class="px-4 py-2 bg-indigo-600 text-white font-semibold rounded-md shadow-md hover:bg-indigo-700 transition duration-150">
                    Login
                </a>

                <a href="/register" class="px-4 py-2 bg-gray-600 text-white font-semibold rounded-md shadow-md hover:bg-gray-700 transition duration-150">
                    Register
                </a>
            </div>
        <% } %>
      </div>
    </div>
</body>
</html>
